/generate  
Set up Entity Framework Core in a .NET 6 Web API project with:  
- A `MaritimeMessage` model (Id, Sender, VesselId, Content, SentTime).  
- DbContext (`AppDbContext`) using SQL Server (or SQLite for simplicity).  
- Database migrations configured.  